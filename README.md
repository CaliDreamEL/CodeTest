# CodeTest

这个仓库用于保存 Java 学习练习和 Dnoong 摄影作品集。

## 目录

- `Test3/`：Java 学习主项目，源码位于 `Test3/src/`。
- `Dnoong-portfolio/`：无需安装依赖的静态摄影作品集。

## IntelliJ IDEA

请直接用 IntelliJ IDEA 打开 `Test3/`，并将 `Test3/src` 标记为 Sources Root。项目配置文件（`.idea`、`.iml`）不提交到 GitHub，由 IDEA 在本机生成。